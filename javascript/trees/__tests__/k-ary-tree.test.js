'use strict';

const { KaryNode, KaryTree, fizzBuzzTree } = require('../index.js'); // I had to bring the function in for some reason?

describe('Testing K-ary tree methods', () => {
  test('FizzBuzzTree should modify tree values correctly', () => {
    // I've gone well over an hour and no test I tried worked so I give up

  });
});
